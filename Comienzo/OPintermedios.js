// operadores de comparacion

igualdad ="x == y"
igualdadmasstricta="x === y" // identifica tipo de dato y verifica si es estrictamente igual
desigual="x != y"
desigualstricto="x !== y"  // identifica tipo de dato y verifica si es estrictamente desigual


// operadores logicos. solo con booleanos

let valor = true;
let valor2=true;
let num1=1;
let num2=2;

let result= valor && valor2; // las dos condiciones tienen que ser verdaderas es lo mismo que ( and)
let resultado=n1<num2;
let resultado1=n1!=num2;

let result1 = valor || valor2; // a menos de que las dos condiciones verdad o por lo menos una sea verdad devolvera= verdad y si los dos son falsos ahi recien devolvera = falso

let result2= !valor; // convertir a falso o verdadero ej= si esto es verdadero bueno con el signo cambiamelo a falso
